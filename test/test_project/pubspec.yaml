name: test_project
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  new_version:
      path: ../../../new_version
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter